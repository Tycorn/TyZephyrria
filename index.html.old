<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ty Zephyrria - Micro-entreprise</title>
    <!-- Tailwind CSS pour le style (via CDN) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Icons Phosphor (via CDN) pour les icônes -->
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    
    <style>
        /* Styles personnalisés supplémentaires */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .fade-in {
            animation: fadeIn 0.5s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .nav-link.active {
            color: #115e59; /* text-teal-800 */
            background-color: #f0fdfa; /* bg-teal-50 */
            font-weight: 600;
        }
        /* Masquer les sections par défaut */
        .tab-content {
            display: none;
        }
        /* Afficher la section active */
        .tab-content.active {
            display: block;
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-800 flex flex-col min-h-screen">

    <!-- HEADER / NAVIGATION -->
    <header class="bg-white sticky top-0 z-50 shadow-sm border-b border-gray-100">
        <div class="container mx-auto px-4 h-16 flex items-center justify-between">
            <!-- Logo -->
            <div class="flex items-center gap-2 cursor-pointer" onclick="openTab('home')">
                <div class="w-8 h-8 bg-teal-800 rounded-tr-xl rounded-bl-xl"></div>
                <span class="font-serif text-2xl font-bold text-teal-800">Ty Zephyrria</span>
            </div>

            <!-- Menu Desktop -->
            <nav class="hidden lg:flex items-center gap-1">
                <button onclick="openTab('artisanat')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-hammer text-lg"></i> Artisanat
                </button>
                <button onclick="openTab('livres')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-book-open text-lg"></i> Livres
                </button>
                <button onclick="openTab('photos')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-camera text-lg"></i> Photos
                </button>
                <button onclick="openTab('graphisme')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-paint-brush text-lg"></i> Graphisme
                </button>
                <button onclick="openTab('chant')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-microphone text-lg"></i> Chant
                </button>
                <button onclick="openTab('sante')" class="nav-link px-3 py-2 rounded-md text-sm font-medium text-gray-600 hover:text-teal-700 hover:bg-gray-50 flex items-center gap-2 transition-colors">
                    <i class="ph ph-heartbeat text-lg"></i> E-Santé
                </button>
            </nav>

            <!-- Bouton Menu Mobile -->
            <button class="lg:hidden p-2 text-gray-600 focus:outline-none" onclick="toggleMobileMenu()">
                <i class="ph ph-list text-2xl"></i>
            </button>
        </div>

        <!-- Menu Mobile (Caché par défaut) -->
        <div id="mobile-menu" class="hidden lg:hidden bg-white border-t border-gray-100 absolute w-full shadow-lg">
            <div class="p-4 space-y-2">
                <button onclick="openTab('artisanat'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-hammer text-lg"></i> Artisanat
                </button>
                <button onclick="openTab('livres'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-book-open text-lg"></i> Livres
                </button>
                <button onclick="openTab('photos'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-camera text-lg"></i> Photos
                </button>
                <button onclick="openTab('graphisme'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-paint-brush text-lg"></i> Graphisme
                </button>
                <button onclick="openTab('chant'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-microphone text-lg"></i> Chant
                </button>
                <button onclick="openTab('sante'); toggleMobileMenu()" class="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3">
                    <i class="ph ph-heartbeat text-lg"></i> E-Santé
                </button>
            </div>
        </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="flex-grow container mx-auto px-4 py-8 md:py-12 relative">
        
        <!-- ACCUEIL -->
        <section id="home" class="tab-content active fade-in">
            <div class="text-center py-12">
                <h1 class="text-5xl md:text-6xl font-serif text-teal-900 mb-6 tracking-tight">Ty Zephyrria</h1>
                <p class="text-xl md:text-2xl text-gray-600 max-w-2xl mx-auto mb-10 font-light">
                    Une micro-entreprise aux multiples facettes. <br/>
                    De la matière brute au numérique, de la voix au soin.
                </p>
                <div class="grid grid-cols-2 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
                    <!-- Cartes Navigation Rapide -->
                    <div onclick="openTab('artisanat')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-hammer text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Création Artisanale</span>
                    </div>
                    <div onclick="openTab('livres')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-book-open text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Livres</span>
                    </div>
                    <div onclick="openTab('photos')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-camera text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Photos</span>
                    </div>
                    <div onclick="openTab('graphisme')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-paint-brush text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Graphisme</span>
                    </div>
                    <div onclick="openTab('chant')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-microphone text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Chant</span>
                    </div>
                    <div onclick="openTab('sante')" class="cursor-pointer p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group">
                        <i class="ph ph-heartbeat text-4xl text-teal-600 group-hover:scale-110 transition-transform"></i>
                        <span class="font-medium text-gray-800">Consulting Santé</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- ARTISANAT -->
        <section id="artisanat" class="tab-content fade-in">
            <div class="text-center max-w-2xl mx-auto mb-8">
                <h2 class="text-3xl font-serif text-teal-800 mb-4">L'Atelier de Ty Zephyrria</h2>
                <p class="text-gray-600">Bijoux, gravures et objets uniques façonnés à la main.</p>
            </div>
            
            <!-- Filtres -->
            <div class="flex flex-wrap justify-center gap-4 mb-8">
                <button onclick="filterArtisanat('all')" class="filter-btn px-4 py-2 rounded-lg font-medium bg-teal-700 text-white hover:bg-teal-800 transition-colors" data-filter="all">Tout voir</button>
                <button onclick="filterArtisanat('bijoux')" class="filter-btn px-4 py-2 rounded-lg font-medium border-2 border-teal-700 text-teal-700 hover:bg-teal-50 transition-colors" data-filter="bijoux">Bijoux Artisanaux</button>
                <button onclick="filterArtisanat('gravure')" class="filter-btn px-4 py-2 rounded-lg font-medium border-2 border-teal-700 text-teal-700 hover:bg-teal-50 transition-colors" data-filter="gravure">Gravures</button>
                <button onclick="filterArtisanat('divers')" class="filter-btn px-4 py-2 rounded-lg font-medium border-2 border-teal-700 text-teal-700 hover:bg-teal-50 transition-colors" data-filter="divers">Divers</button>
            </div>

            <!-- Grille de produits -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Items Bijoux -->
                <div class="artisanat-item bijoux bg-white rounded-xl shadow-lg overflow-hidden border border-gray-100 flex flex-col">
                    <img src="https://placehold.co/400x300/e0f2f1/004d40?text=Collier+Écume" alt="Bijou" class="w-full h-48 object-cover">
                    <div class="p-5 flex-grow">
                        <span class="text-xs font-bold text-teal-800 uppercase">Bijoux</span>
                        <h3 class="text-xl font-bold text-gray-800 mb-2">Collier Écume</h3>
                        <p class="text-gray-600 text-sm mb-4">Argent massif et verre poli.</p>
                        <div class="text-teal-700 font-bold text-lg">45€</div>
                    </div>
                </div>
                <div class="artisanat-item bijoux bg-white rounded-xl shadow-lg overflow-hidden border border-gray-100 flex flex-col">
                    <img src="https://placehold.co/400x300/e0f2f1/004d40?text=Boucles+Zéphyr" alt="Bijou" class="w-full h-48 object-cover">
                    <div class="p-5 flex-grow">
                        <span class="text-xs font-bold text-teal-800 uppercase">Bijoux</span>
                        <h3 class="text-xl font-bold text-gray-800 mb-2">Boucles Zéphyr</h3>
                        <p class="text-gray-600 text-sm mb-4">Laiton doré à l'or fin.</p>
                        <div class="text-teal-700 font-bold text-lg">32€</div>
                    </div>
                </div>

                <!-- Items Gravure -->
                <div class="artisanat-item gravure bg-white rounded-xl shadow-lg overflow-hidden border border-gray-100 flex flex-col">
                    <img src="https://placehold.co/400x300/f3e5f5/4a148c?text=Estampe+Phare" alt="Gravure" class="w-full h-48 object-cover">
                    <div class="p-5 flex-grow">
                        <span class="text-xs font-bold text-teal-800 uppercase">Gravure</span>
                        <h3 class="text-xl font-bold text-gray-800 mb-2">Estampe Phare</h3>
                        <p class="text-gray-600 text-sm mb-4">Linogravure originale.</p>
                        <div class="text-teal-700 font-bold text-lg">25€</div>
                    </div>
                </div>

                <!-- Items Divers -->
                <div class="artisanat-item divers bg-white rounded-xl shadow-lg overflow-hidden border border-gray-100 flex flex-col">
                    <img src="https://placehold.co/400x300/fff3e0/e65100?text=Mobile+Bois" alt="Divers" class="w-full h-48 object-cover">
                    <div class="p-5 flex-grow">
                        <span class="text-xs font-bold text-teal-800 uppercase">Divers</span>
                        <h3 class="text-xl font-bold text-gray-800 mb-2">Mobile Bois Flotté</h3>
                        <p class="text-gray-600 text-sm mb-4">Suspension décorative naturelle.</p>
                        <div class="text-teal-700 font-bold text-lg">55€</div>
                    </div>
                </div>
            </div>
        </section>

        <!-- LIVRES -->
        <section id="livres" class="tab-content fade-in">
            <div class="max-w-4xl mx-auto">
                <h2 class="text-3xl font-serif text-teal-800 mb-8 text-center">Bibliothèque & Écrits</h2>
                <div class="space-y-8">
                    <!-- Livre 1 -->
                    <div class="flex flex-col md:flex-row bg-white rounded-xl shadow-md overflow-hidden border border-gray-100">
                        <div class="md:w-1/3 bg-gray-200">
                            <img src="https://placehold.co/300x450/fff3e0/e65100?text=Livre+Vol.+1" alt="Couverture" class="w-full h-full object-cover">
                        </div>
                        <div class="md:w-2/3 p-6 flex flex-col justify-center">
                            <div class="uppercase text-sm text-orange-600 font-bold mb-2">Poésie</div>
                            <h3 class="text-2xl font-bold text-gray-800 mb-3">Le Souffle des Marées - Vol. 1</h3>
                            <p class="text-gray-600 mb-6">Une anthologie explorant les mythes côtiers et la réalité insulaire.</p>
                            <button class="w-fit px-4 py-2 bg-teal-700 text-white rounded-lg hover:bg-teal-800">Commander (15€)</button>
                        </div>
                    </div>
                     <!-- Livre 2 -->
                     <div class="flex flex-col md:flex-row bg-white rounded-xl shadow-md overflow-hidden border border-gray-100">
                        <div class="md:w-1/3 bg-gray-200">
                            <img src="https://placehold.co/300x450/fff3e0/e65100?text=Livre+Vol.+2" alt="Couverture" class="w-full h-full object-cover">
                        </div>
                        <div class="md:w-2/3 p-6 flex flex-col justify-center">
                            <div class="uppercase text-sm text-orange-600 font-bold mb-2">Roman</div>
                            <h3 class="text-2xl font-bold text-gray-800 mb-3">Les Ombres du Port</h3>
                            <p class="text-gray-600 mb-6">Un récit policier au cœur des brumes portuaires.</p>
                            <button class="w-fit px-4 py-2 bg-teal-700 text-white rounded-lg hover:bg-teal-800">Commander (18€)</button>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- PHOTOS -->
        <section id="photos" class="tab-content fade-in">
            <div class="flex justify-center mb-8 bg-gray-100 p-1 rounded-full w-max mx-auto">
                <button onclick="switchPhotoMode('artistique')" id="btn-artistique" class="px-6 py-2 rounded-full font-medium transition-all bg-white shadow text-teal-800">Artistique</button>
                <button onclick="switchPhotoMode('prestation')" id="btn-prestation" class="px-6 py-2 rounded-full font-medium transition-all text-gray-500 hover:text-gray-700">Prestations</button>
            </div>

            <!-- Mode Artistique -->
            <div id="view-artistique" class="columns-1 md:columns-2 lg:columns-3 gap-4 space-y-4">
                <div class="break-inside-avoid rounded-lg overflow-hidden"><img src="https://placehold.co/400x500/263238/ffffff?text=Art+1" class="w-full rounded-lg hover:opacity-90 transition"></div>
                <div class="break-inside-avoid rounded-lg overflow-hidden"><img src="https://placehold.co/400x300/263238/ffffff?text=Art+2" class="w-full rounded-lg hover:opacity-90 transition"></div>
                <div class="break-inside-avoid rounded-lg overflow-hidden"><img src="https://placehold.co/400x600/263238/ffffff?text=Art+3" class="w-full rounded-lg hover:opacity-90 transition"></div>
                <div class="break-inside-avoid rounded-lg overflow-hidden"><img src="https://placehold.co/400x400/263238/ffffff?text=Art+4" class="w-full rounded-lg hover:opacity-90 transition"></div>
            </div>

            <!-- Mode Prestation -->
            <div id="view-prestation" class="hidden grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
                <div class="bg-white p-8 rounded-xl shadow-lg border-t-4 border-teal-600">
                    <h3 class="text-2xl font-bold mb-4">Séance Portrait</h3>
                    <ul class="space-y-3 text-gray-600 mb-6">
                        <li>• 1h de shooting</li>
                        <li>• 10 photos retouchées HD</li>
                        <li>• Galerie privée</li>
                    </ul>
                    <p class="text-3xl font-bold text-teal-800 mb-6">150€</p>
                    <button class="w-full px-4 py-2 bg-teal-700 text-white rounded-lg">Réserver</button>
                </div>
                <div class="bg-white p-8 rounded-xl shadow-lg border-t-4 border-orange-400">
                    <h3 class="text-2xl font-bold mb-4">Événementiel</h3>
                    <ul class="space-y-3 text-gray-600 mb-6">
                        <li>• Demi-journée (4h)</li>
                        <li>• Reportage complet</li>
                        <li>• Post-traitement soigné</li>
                    </ul>
                    <p class="text-3xl font-bold text-gray-800 mb-6">Sur devis</p>
                    <button class="w-full px-4 py-2 border-2 border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50">Contacter</button>
                </div>
            </div>
        </section>

        <!-- GRAPHISME -->
        <section id="graphisme" class="tab-content fade-in">
            <div class="text-center mb-10">
                <h2 class="text-3xl font-serif text-teal-800 mb-4">Identité Visuelle & Design</h2>
                <p class="text-gray-600 max-w-2xl mx-auto">Logos, maquettes, illustrations.</p>
            </div>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                <div class="aspect-square bg-purple-50 rounded-lg flex flex-col items-center justify-center p-4 hover:bg-purple-100 transition cursor-pointer">
                    <i class="ph ph-pen-nib text-5xl text-purple-700 mb-2"></i>
                    <span class="font-bold text-purple-900">Logo</span>
                </div>
                <div class="aspect-square bg-purple-50 rounded-lg flex flex-col items-center justify-center p-4 hover:bg-purple-100 transition cursor-pointer">
                    <i class="ph ph-article text-5xl text-purple-700 mb-2"></i>
                    <span class="font-bold text-purple-900">Flyer</span>
                </div>
                <div class="aspect-square bg-purple-50 rounded-lg flex flex-col items-center justify-center p-4 hover:bg-purple-100 transition cursor-pointer">
                    <i class="ph ph-desktop text-5xl text-purple-700 mb-2"></i>
                    <span class="font-bold text-purple-900">Web</span>
                </div>
                <div class="aspect-square bg-purple-50 rounded-lg flex flex-col items-center justify-center p-4 hover:bg-purple-100 transition cursor-pointer">
                    <i class="ph ph-paint-brush-broad text-5xl text-purple-700 mb-2"></i>
                    <span class="font-bold text-purple-900">Art</span>
                </div>
            </div>
        </section>

        <!-- CHANT -->
        <section id="chant" class="tab-content fade-in">
            <div class="max-w-3xl mx-auto">
                <div class="bg-gradient-to-br from-indigo-900 to-purple-800 text-white p-10 rounded-2xl shadow-xl mb-8 text-center">
                    <i class="ph ph-microphone-stage text-6xl text-purple-300 mb-4"></i>
                    <h2 class="text-3xl font-serif mb-2">Voix & Interprétation</h2>
                    <p class="text-indigo-200">Concerts, Événements privés, Coaching vocal</p>
                </div>
                <div class="space-y-4">
                    <div class="flex items-center bg-white p-4 rounded-lg shadow border border-gray-100">
                        <div class="bg-indigo-100 p-3 rounded-full mr-4 text-indigo-700"><i class="ph ph-music-note"></i></div>
                        <div class="flex-grow">
                            <h4 class="font-bold text-gray-800">Demo Jazz Standard</h4>
                        </div>
                        <button class="p-2 bg-indigo-600 text-white rounded-full hover:bg-indigo-700 text-sm">Play ▶</button>
                    </div>
                    <div class="flex items-center bg-white p-4 rounded-lg shadow border border-gray-100">
                        <div class="bg-indigo-100 p-3 rounded-full mr-4 text-indigo-700"><i class="ph ph-music-note"></i></div>
                        <div class="flex-grow">
                            <h4 class="font-bold text-gray-800">Demo Chant Lyrique</h4>
                        </div>
                        <button class="p-2 bg-indigo-600 text-white rounded-full hover:bg-indigo-700 text-sm">Play ▶</button>
                    </div>
                </div>
            </div>
        </section>

        <!-- E-SANTE -->
        <section id="sante" class="tab-content fade-in">
            <div class="flex flex-col md:flex-row items-center gap-8 mb-12">
                <div class="md:w-1/2">
                    <h2 class="text-3xl font-serif text-teal-800 mb-6">Consulting Santé & Numérique</h2>
                    <p class="text-gray-700 leading-relaxed mb-4">
                        Allier expertise médicale et innovation numérique. J'accompagne les professionnels et les structures dans leur transition digitale et l'optimisation de leurs parcours de soins.
                    </p>
                    <button class="px-6 py-3 bg-teal-700 text-white rounded-lg hover:bg-teal-800 font-bold shadow-lg mt-4">Prendre Rendez-vous</button>
                </div>
                <div class="md:w-1/2 bg-blue-50 rounded-2xl p-8 border border-blue-100 w-full">
                    <h3 class="font-bold text-lg mb-4 flex items-center gap-2 text-blue-800">
                        <i class="ph ph-check-circle"></i> Domaines d'expertise
                    </h3>
                    <ul class="space-y-3">
                        <li class="flex items-center gap-3 bg-white p-3 rounded shadow-sm"><span class="text-green-500">✓</span> Télémédecine</li>
                        <li class="flex items-center gap-3 bg-white p-3 rounded shadow-sm"><span class="text-green-500">✓</span> Gestion de projet e-Santé</li>
                        <li class="flex items-center gap-3 bg-white p-3 rounded shadow-sm"><span class="text-green-500">✓</span> Rédaction médicale</li>
                        <li class="flex items-center gap-3 bg-white p-3 rounded shadow-sm"><span class="text-green-500">✓</span> Formation des équipes</li>
                    </ul>
                </div>
            </div>
        </section>

    </main>

    <!-- FOOTER -->
    <footer class="bg-slate-900 text-slate-300 py-12">
        <div class="container mx-auto px-4 text-center">
            <h4 class="text-xl font-serif text-white mb-4">Ty Zephyrria</h4>
            <div class="flex justify-center gap-6 mb-8">
                <i class="ph ph-instagram-logo text-2xl hover:text-white cursor-pointer"></i>
                <i class="ph ph-facebook-logo text-2xl hover:text-white cursor-pointer"></i>
                <i class="ph ph-linkedin-logo text-2xl hover:text-white cursor-pointer"></i>
            </div>
            <div class="text-sm text-slate-500">
                © <span id="year"></span> Ty Zephyrria. Tous droits réservés.
            </div>
        </div>
    </footer>

    <!-- JAVASCRIPT LOGIC -->
    <script>
        // Mettre à jour l'année
        document.getElementById('year').textContent = new Date().getFullYear();

        // Fonction pour changer d'onglet principal
        function openTab(tabId) {
            // Cacher toutes les sections
            const contents = document.querySelectorAll('.tab-content');
            contents.forEach(content => content.classList.remove('active'));

            // Enlever l'état actif des boutons
            const buttons = document.querySelectorAll('.nav-link');
            // Note: simple reset, pour un style plus poussé on pourrait ajouter des ID aux boutons
            
            // Afficher la section demandée
            const selectedTab = document.getElementById(tabId);
            if(selectedTab) {
                selectedTab.classList.add('active');
                // Scroll to top
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        }

        // Menu Mobile
        function toggleMobileMenu() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        }

        // Filtre Artisanat
        function filterArtisanat(category) {
            const items = document.querySelectorAll('.artisanat-item');
            const buttons = document.querySelectorAll('.filter-btn');

            // Gérer le style des boutons
            buttons.forEach(btn => {
                if(btn.dataset.filter === category) {
                    btn.classList.remove('bg-transparent', 'text-teal-700', 'border-2');
                    btn.classList.add('bg-teal-700', 'text-white');
                } else {
                    btn.classList.add('bg-transparent', 'text-teal-700', 'border-2');
                    btn.classList.remove('bg-teal-700', 'text-white');
                }
            });

            // Filtrer les items
            items.forEach(item => {
                if (category === 'all' || item.classList.contains(category)) {
                    item.style.display = 'flex'; // ou block selon votre layout, flex ici pour la card
                } else {
                    item.style.display = 'none';
                }
            });
        }

        // Switch Photos
        function switchPhotoMode(mode) {
            const artistView = document.getElementById('view-artistique');
            const prestaView = document.getElementById('view-prestation');
            const btnArt = document.getElementById('btn-artistique');
            const btnPresta = document.getElementById('btn-prestation');

            if(mode === 'artistique') {
                artistView.classList.remove('hidden');
                prestaView.classList.add('hidden');
                
                btnArt.classList.add('bg-white', 'shadow', 'text-teal-800');
                btnArt.classList.remove('text-gray-500');
                
                btnPresta.classList.remove('bg-white', 'shadow', 'text-teal-800');
                btnPresta.classList.add('text-gray-500');
            } else {
                artistView.classList.add('hidden');
                prestaView.classList.remove('hidden');

                btnPresta.classList.add('bg-white', 'shadow', 'text-teal-800');
                btnPresta.classList.remove('text-gray-500');
                
                btnArt.classList.remove('bg-white', 'shadow', 'text-teal-800');
                btnArt.classList.add('text-gray-500');
            }
        }
    </script>
</body>
</html>er', 'Web', 'Illustration'].map((item, i) => (
        <div key={i} className="aspect-square bg-gray-100 rounded-lg flex items-center justify-center relative overflow-hidden group cursor-pointer">
           <img 
            src={`https://placehold.co/300x300/f3e5f5/7b1fa2?text=${item}`} 
            alt={item} 
            className="w-full h-full object-cover transition-transform duration-300 group-hover:scale-110"
          />
           <div className="absolute inset-0 bg-teal-900/60 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
             <span className="text-white font-bold text-lg">{item}</span>
           </div>
        </div>
      ))}
    </div>
    <div className="mt-12 bg-teal-50 p-8 rounded-xl text-center">
      <h3 className="text-xl font-bold mb-4">Un projet en tête ?</h3>
      <Button variant="primary">Demander un devis graphisme</Button>
    </div>
  </div>
);

// 5. Chant
const ChantSection = () => (
  <div className="max-w-3xl mx-auto animate-fadeIn">
    <div className="bg-gradient-to-br from-indigo-900 to-purple-800 text-white p-10 rounded-2xl shadow-xl mb-8 text-center">
      <Mic2 className="w-16 h-16 mx-auto mb-4 text-purple-300" />
      <h2 className="text-3xl font-serif mb-2">Voix & Interprétation</h2>
      <p className="text-indigo-200">Concerts, Événements privés, Coaching vocal</p>
    </div>

    <div className="space-y-6">
      <h3 className="text-xl font-bold text-gray-800 border-b pb-2">Démos Audio</h3>
      {[1, 2, 3].map((track) => (
        <div key={track} className="flex items-center bg-white p-4 rounded-lg shadow border border-gray-100">
          <div className="bg-indigo-100 p-3 rounded-full mr-4 text-indigo-700">
            <Music size={20} />
          </div>
          <div className="flex-grow">
            <h4 className="font-bold text-gray-800">Titre de la démo #{track}</h4>
            <p className="text-sm text-gray-500">Jazz / Blues Standard</p>
          </div>
          <div className="text-gray-400 text-sm">03:45</div>
          <button className="ml-4 p-2 bg-indigo-600 text-white rounded-full hover:bg-indigo-700">
            ▶
          </button>
        </div>
      ))}
    </div>
  </div>
);

// 6. E-Santé & Consulting
const SanteSection = () => (
  <div className="animate-fadeIn">
    <div className="flex flex-col md:flex-row items-center gap-8 mb-12">
      <div className="md:w-1/2">
        <h2 className="text-3xl font-serif text-teal-800 mb-6">Consulting Santé & Numérique</h2>
        <p className="text-gray-700 leading-relaxed mb-4">
          Allier expertise médicale et innovation numérique. J'accompagne les professionnels et les structures dans leur transition digitale et l'optimisation de leurs parcours de soins.
        </p>
        <div className="flex gap-4 mt-6">
          <Button variant="primary">Prendre RDV</Button>
          <Button variant="outline">Mon profil LinkedIn</Button>
        </div>
      </div>
      <div className="md:w-1/2 bg-blue-50 rounded-2xl p-8 border border-blue-100">
        <h3 className="font-bold text-lg mb-4 flex items-center gap-2 text-blue-800">
          <Activity size={20} /> Domaines d'expertise
        </h3>
        <ul className="space-y-3">
          {['Télémédecine & Télésoin', 'Gestion de projet e-Santé', 'Rédaction médicale', 'Formation des équipes'].map(item => (
            <li key={item} className="flex items-center gap-3 bg-white p-3 rounded shadow-sm">
              <span className="text-green-500">✓</span> {item}
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>
);

// --- COMPOSANT PRINCIPAL ---

export default function TyZephyrriaApp() {
  const [activeTab, setActiveTab] = useState('home');
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  const navItems = [
    { id: 'artisanat', label: 'Création Artisanale', icon: <Hammer size={18} /> },
    { id: 'livres', label: 'Livres', icon: <BookOpen size={18} /> },
    { id: 'photos', label: 'Photos', icon: <Camera size={18} /> },
    { id: 'graphisme', label: 'Graphisme', icon: <Palette size={18} /> },
    { id: 'chant', label: 'Chant', icon: <Mic2 size={18} /> },
    { id: 'sante', label: 'E-santé', icon: <HeartPulse size={18} /> },
  ];

  const renderContent = () => {
    switch (activeTab) {
      case 'artisanat': return <ArtisanatSection />;
      case 'livres': return <LivresSection />;
      case 'photos': return <PhotoSection />;
      case 'graphisme': return <GraphismeSection />;
      case 'chant': return <ChantSection />;
      case 'sante': return <SanteSection />;
      default: return (
        <div className="text-center py-12 animate-fadeIn">
          <h1 className="text-5xl md:text-6xl font-serif text-teal-900 mb-6 tracking-tight">Ty Zephyrria</h1>
          <p className="text-xl md:text-2xl text-gray-600 max-w-2xl mx-auto mb-10 font-light">
            Une micro-entreprise aux multiples facettes. <br/>
            De la matière brute au numérique, de la voix au soin.
          </p>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
             {navItems.map(item => (
               <button 
                 key={item.id}
                 onClick={() => setActiveTab(item.id)}
                 className="p-6 bg-white rounded-xl shadow-md hover:shadow-xl transition-all border border-transparent hover:border-teal-200 flex flex-col items-center gap-3 group"
               >
                 <div className="text-teal-600 group-hover:scale-110 transition-transform">{React.cloneElement(item.icon, { size: 32 })}</div>
                 <span className="font-medium text-gray-800">{item.label}</span>
               </button>
             ))}
          </div>
        </div>
      );
    }
  };

  return (
    <div className="min-h-screen bg-slate-50 font-sans text-gray-800 flex flex-col">
      {/* Header */}
      <header className="bg-white sticky top-0 z-50 shadow-sm border-b border-gray-100">
        <div className="container mx-auto px-4 h-16 flex items-center justify-between">
          <div 
            className="font-serif text-2xl font-bold text-teal-800 cursor-pointer flex items-center gap-2"
            onClick={() => setActiveTab('home')}
          >
            <div className="w-8 h-8 bg-teal-800 rounded-tr-xl rounded-bl-xl"></div>
            Ty Zephyrria
          </div>

          {/* Desktop Nav */}
          <nav className="hidden lg:flex items-center gap-1">
            {navItems.map(item => (
              <button
                key={item.id}
                onClick={() => setActiveTab(item.id)}
                className={`px-3 py-2 rounded-md text-sm font-medium transition-colors flex items-center gap-2 ${activeTab === item.id ? 'bg-teal-50 text-teal-800' : 'text-gray-600 hover:text-teal-700 hover:bg-gray-50'}`}
              >
                {item.icon}
                {item.label}
              </button>
            ))}
            <button 
               className="ml-4 px-4 py-2 bg-teal-800 text-white rounded-md text-sm hover:bg-teal-900 transition-colors"
               onClick={() => alert("Formulaire de contact à intégrer ici")}
            >
              Contact
            </button>
          </nav>

          {/* Mobile Menu Button */}
          <button className="lg:hidden p-2 text-gray-600" onClick={() => setIsMenuOpen(!isMenuOpen)}>
            {isMenuOpen ? <X /> : <Menu />}
          </button>
        </div>

        {/* Mobile Nav */}
        {isMenuOpen && (
          <nav className="lg:hidden bg-white border-t border-gray-100 absolute w-full shadow-lg">
            <div className="p-4 space-y-2">
              <button 
                onClick={() => { setActiveTab('home'); setIsMenuOpen(false); }}
                className="w-full text-left px-4 py-3 rounded-lg hover:bg-gray-50 font-medium flex items-center gap-3"
              >
                <Home size={20} /> Accueil
              </button>
              {navItems.map(item => (
                <button
                  key={item.id}
                  onClick={() => { setActiveTab(item.id); setIsMenuOpen(false); }}
                  className={`w-full text-left px-4 py-3 rounded-lg flex items-center gap-3 font-medium ${activeTab === item.id ? 'bg-teal-50 text-teal-800' : 'text-gray-600 hover:bg-gray-50'}`}
                >
                  {item.icon}
                  {item.label}
                </button>
              ))}
            </div>
          </nav>
        )}
      </header>

      {/* Main Content */}
      <main className="flex-grow container mx-auto px-4 py-8 md:py-12">
        {renderContent()}
      </main>

      {/* Footer */}
      <footer className="bg-slate-900 text-slate-300 py-12">
        <div className="container mx-auto px-4 grid grid-cols-1 md:grid-cols-3 gap-8">
          <div>
            <h4 className="text-xl font-serif text-white mb-4 flex items-center gap-2">
               <div className="w-6 h-6 bg-teal-500 rounded-tr-lg rounded-bl-lg"></div>
               Ty Zephyrria
            </h4>
            <p className="text-sm leading-relaxed mb-4">
              Micro-entreprise polyvalente basée en Bretagne.
              Création, Art et Soin.
            </p>
            <div className="flex gap-4">
              <Instagram className="w-5 h-5 cursor-pointer hover:text-white" />
              <Facebook className="w-5 h-5 cursor-pointer hover:text-white" />
              <Linkedin className="w-5 h-5 cursor-pointer hover:text-white" />
            </div>
          </div>
          <div>
            <h4 className="text-white font-bold mb-4">Liens Rapides</h4>
            <ul className="space-y-2 text-sm">
              <li className="hover:text-teal-400 cursor-pointer" onClick={() => setActiveTab('artisanat')}>Boutique Artisanale</li>
              <li className="hover:text-teal-400 cursor-pointer" onClick={() => setActiveTab('sante')}>Services Santé</li>
              <li className="hover:text-teal-400 cursor-pointer" onClick={() => setActiveTab('chant')}>Booking Concert</li>
              <li className="hover:text-teal-400 cursor-pointer">Mentions Légales</li>
            </ul>
          </div>
          <div>
            <h4 className="text-white font-bold mb-4">Contact</h4>
            <ul className="space-y-2 text-sm">
              <li className="flex items-center gap-2"><Mail size={16}/> contact@tyzephyrria.bzh</li>
              <li>SIRET: 123 456 789 00000</li>
              <li>Bretagne, France</li>
            </ul>
          </div>
        </div>
        <div className="border-t border-slate-800 mt-8 pt-8 text-center text-xs text-slate-500">
          © {new Date().getFullYear()} Ty Zephyrria. Tous droits réservés.
        </div>
      </footer>
    </div>
  );
}
